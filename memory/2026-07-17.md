# 2026-07-17

- Cloned `TGambit65/plumber_app` into this workspace and launched the app locally.
- The verified local web URL is `http://localhost:3000`; it runs as an optimized Next.js production server.
- PostgreSQL 16 runs in the persistent Docker container `plumber-app-aa-postgres`, publishing only to `127.0.0.1:55432`, with volume `plumber-app-aa-postgres-data`.
- The app connects with a non-superuser, non-BYPASSRLS `plumber` role. Setup order was current schema push, demo seed, `src/db/rls.sql` as the database superuser, then `src/db/sync.sql`.
- RLS verification covers 51 tenant tables. Unscoped application-role reads return zero rows, while the security-definer login bootstrap works.
- Current primary demo login is `owner@plumbzebra.demo` (password lives in the demo seed data, not in this note). The `@apexplumbing.demo` accounts in `web/README.md` and some older smoke scripts are stale.
- Verification passed: 54 unit tests, production build, all four role logins and representative pages, seeded customer detail, technician initial-sync API, PWA manifest, and service worker.
- Tailscale Serve exposes the app tailnet-only on port 3000 (hostname redacted from this public repo), proxying to localhost:3000. HTTPS and an authenticated admin login were verified. Disable only this route with `tailscale serve --https=3000 off`.
